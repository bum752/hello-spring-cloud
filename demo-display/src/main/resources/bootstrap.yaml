spring:
  cloud:
    vault:
      uri: http://localhost:8200
      connection-timeout: 5000
      read-timeout: 1500
      config:
        order: -10
      authentication: token
      token: ${VAULT_TOKEN}
